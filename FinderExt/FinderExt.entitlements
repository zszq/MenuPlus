<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
        FinderExt 在 macOS Sequoia + ad-hoc 签名下，必须启用 App Sandbox 才能被
        PluginKit 收录，否则右键完全看不到菜单。

        启用 sandbox 后，需要 user-selected.read-write 权限来支持 createFile 等
        写入操作。注意：sandbox 模式下只有 selectedItemURLs() 显式选中的 item
        才自动获得 user-selected 权限，targetedURL()（当前显示目录）没有这个权限，
        因此动作 5/6（新建文件）在 FinderSync.swift 中限制为仅在用户明确右键
        文件夹时显示。

        动作 1（在终端中打开）已改为 NSWorkspace.open + LaunchServices，由系统将
        URL 派发给 Terminal.app，不再需要 Apple Events 权限。
    -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- 允许读写用户明确选中的文件 / 文件夹（动作 5/6 createFile 需要） -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!--
        Phase 0 验证结论：ad-hoc 签名直接拒绝 application-groups entitlement
        （Xcode 报 "requires a provisioning profile"），App Group 主路线在
        当前签名环境下不可行。如未来接入 Apple Developer 账号，可恢复：

        <key>com.apple.security.application-groups</key>
        <array>
            <string>group.zl.MenuPlus</string>
        </array>
    -->

    <!--
        只读主 App 写出的共享配置（"用 XX 打开" 应用列表）。

        为什么不用 App Group：见上方注释，ad-hoc 构建期被拒。
        为什么不读主 App 沙盒容器：macOS 对其他 App 的沙盒容器有系统级保护，
        FinderSync 监控目录的读授权覆盖不到，实测读取被拒
        （"未能打开文件…因为你没有查看它的权限"）。

        故主 App 改写真实 home 下的中立路径 ~/Library/Application Support/MenuPlus/，
        扩展用 temporary-exception 只读打通（普通沙盒策略 entitlement，无需
        provisioning profile，ad-hoc 可用——与 application-groups 不同）。

        ⚠️ MAS 分发前必须迁移到 App Group 并删除此 exception
        （temporary-exception 是 App Store 审核高风险项）。
    -->
    <key>com.apple.security.temporary-exception.files.home-relative-path.read-only</key>
    <array>
        <string>/Library/Application Support/MenuPlus/</string>
    </array>
</dict>
</plist>
